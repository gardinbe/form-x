import { max } from './max';
import { maxLen } from './max-len';
import { min } from './min';
import { minLen } from './min-len';
import { pattern } from './pattern';
import { presetPattern } from './preset-pattern';
import { required } from './required';

export const Validators = {
  max,
  maxLen,
  min,
  minLen,
  pattern,
  presetPattern,
  required
};
